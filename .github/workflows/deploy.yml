name: Build and deploy

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "backend/**"
      - "kubernetes/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-dev
  cancel-in-progress: true

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  CLUSTER_NAME: cloudops-academy-dev
  FRONTEND_REPOSITORY: cloudops-academy-dev-frontend
  BACKEND_REPOSITORY: cloudops-academy-dev-backend

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push images
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t "$REGISTRY/$FRONTEND_REPOSITORY:$IMAGE_TAG" frontend
          docker build -t "$REGISTRY/$BACKEND_REPOSITORY:$IMAGE_TAG" backend
          docker push "$REGISTRY/$FRONTEND_REPOSITORY:$IMAGE_TAG"
          docker push "$REGISTRY/$BACKEND_REPOSITORY:$IMAGE_TAG"

      - name: Connect to EKS
        run: aws eks update-kubeconfig --name "$CLUSTER_NAME" --region "$AWS_REGION"

      - name: Create database secret
        run: |
          secret_json="$(aws secretsmanager get-secret-value --secret-id cloudops-academy-dev/database --query SecretString --output text)"
          kubectl apply -f kubernetes/namespace.yml
          kubectl -n cloudops-academy create secret generic database \
            --from-literal=PGHOST="$(jq -r .PGHOST <<<"$secret_json")" \
            --from-literal=PGPORT="$(jq -r .PGPORT <<<"$secret_json")" \
            --from-literal=PGDATABASE="$(jq -r .PGDATABASE <<<"$secret_json")" \
            --from-literal=PGUSER="$(jq -r .PGUSER <<<"$secret_json")" \
            --from-literal=PGPASSWORD="$(jq -r .PGPASSWORD <<<"$secret_json")" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Initialize database
        run: |
          kubectl -n cloudops-academy delete job database-init --ignore-not-found
          kubectl apply -f kubernetes/database-init-job.yml
          kubectl -n cloudops-academy wait --for=condition=complete job/database-init --timeout=180s

      - name: Deploy application
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          sed "s|BACKEND_IMAGE|$REGISTRY/$BACKEND_REPOSITORY:$IMAGE_TAG|g" kubernetes/backend.yml | kubectl apply -f -
          sed "s|FRONTEND_IMAGE|$REGISTRY/$FRONTEND_REPOSITORY:$IMAGE_TAG|g" kubernetes/frontend.yml | kubectl apply -f -
          kubectl -n cloudops-academy rollout status deployment/backend --timeout=180s
          kubectl -n cloudops-academy rollout status deployment/frontend --timeout=180s
